class JobModelClass:
    def __init__(self, job_id, job_desc, min_lvl, max_lvl):

        self._jobId = job_id
        self._jobDesc = job_desc
        self._minLVL = min_lvl
        self._maxLVL = max_lvl

